# 気配盤（kiseiban.html）開発の引き継ぎプロンプト

このファイルの内容をそのままClaude Codeの最初のメッセージとして貼り付けてください。
`kiseiban.html` 本体も一緒にリポジトリに置いておくと、Claude Codeがすぐに読み込めます。

---

## プロンプト本文（ここから）

`kiseiban.html` という単一HTMLファイルのFX/バイナリーオプション向け参考ツールを開発中です。以下の仕様を把握した上で、今後の機能追加・修正を手伝ってください。

### 概要
- USD/JPY, EUR/JPY, GBP/JPY を対象にした、価格チャート・テクニカル指標・エントリー根拠の可視化・トレード記録帳をまとめた単一HTMLファイル
- Vanilla JS + Chart.js（CDN経由）。ビルド不要、ブラウザで直接開ける
- GitHub Pagesでの静的ホスティングを想定（`index.html`としてルートに配置）

### データソースの制約（重要）
- 無料・APIキー不要の `https://www.freeforexapi.com/api/live?pairs=USDJPY,EURJPY,GBPJPY` を20秒間隔でポーリングし、現在レートのみ取得
- **過去の時系列データは取得できない**ため、ポーリングした値を自前で1分足OHLCとして蓄積し、localStorageに永続化している（キー: `kiseiban:candles1m:<PAIR>`）
- 表示用の5/15/30/60分足、および戦略判定用のM15/H4足は、この1分足を`resample()`関数で集計して作っている
- そのため、意味のあるH4足の水準検出には数日分のデータ蓄積が必要（この制約はUIにも明記している）

### 実装済みの指標・シグナル
1. **基本指標**：SMA9 / SMA21 / EMA20 / RSI(14)
2. **シンプル参考シグナル**：SMA9とSMA21のクロス + RSIが50〜70(LONG寄り)/30〜50(SHORT寄り)の帯にあるかで「様子見/HIGH寄り/LOW寄り」を表示
3. **H4×M15戦略シグナル**（ユーザーが実際に使っているトレード手法を反映）：
   - H4足でピボット高値・安値を検出（`findPivots`, window=2）
   - 価格の0.1%を許容誤差として近い水準をクラスタリングし、2回以上反発した水準のみ「重要ライン」として採用（`clusterLevels`）
   - 現在価格に最も近い水準を対象に、M15足の実体（close）がその水準を上抜けたかを確認（ヒゲ抜けは無視）
   - ブレイク後、水準付近まで押し戻り、かつ下ヒゲが長い陽線（ピンバー: 下ヒゲ≧実体×1.5、下ヒゲ/レンジ≧0.5、陽線）が出現したら「条件成立」
   - 利確目安＝直近30本のM15高値、損切り目安＝水準−10pips（JPYペア想定でpip=0.01）
   - 現状は**ロング側のみ実装**。ショート側（下抜け→戻り→上ヒゲの長い陰線）は未実装

### トレード記録帳
- 勝ち/負けをワンタップで記録（金額は任意入力）
- 累積勝率を、ペイアウト率85%を前提とした損益分岐勝率54.1%(=1/1.85)のラインと並べてグラフ表示
- localStorageに永続化（キー: `kiseiban:trades:<PAIR>`）。**FX的な損益pips記録には未対応**、現状は勝敗の二値のみ

### デザイン方針
- ダークな「元帳/端末」的トーン。背景 `#0e1a1c`、パネル `#132427`、アクセントに真鍮色 `#c9a227`
- フォントは数値部分にIBM Plex Mono、それ以外はInter
- 常時表示される警告バナー（「参考情報であり勝敗を保証しない」旨）を画面最上部に固定

### 今後お願いしたいこと（優先度が高い順の例。ユーザーの指示に応じて調整してください）
1. H4×M15戦略のショート側ロジックの実装（ロング側の鏡合わせ）
2. トレード記録帳をFX向けに拡張（勝敗の二値ではなく、pips損益・R倍率を記録できるように）
3. ピンバー判定の閾値をUIから調整できるようにする（現状はコード内に固定値でハードコーディング）
4. freeforexapi.comのポーリングが失敗した場合のリトライ・エラー表示の改善
5. EMA10版（現在はEMA20固定）への切り替えオプション追加

### 注意事項
- このツールは投資助言ではなく、あくまで参考情報の可視化ツールという位置づけを崩さないでください（UIの警告文言・免責表現は維持してください）
- 新機能を追加する際も、「勝敗を保証する」ような断定的な表現は避けてください

## プロンプト本文（ここまで）
